export type QuestionType = {
    id: number;
    title: string;
    problemStatement: string;
    difficultyLevel: string;
    type: string;
    maxScore:number
    tags: string[];
  };

  export type questionIds = {
    questionIds: []
  };
